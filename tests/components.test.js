import { decompose, charactersWithComponent } from '../index';

test('decompose', () => {
	console.log(decompose('鸡'));
});

test('charactersWithComponent', () => {
	console.log(charactersWithComponent('鸟'));
});
